<!--index.wxml-->
<block wx:for="{{goodsList}}" wx:key="title">
<van-card
  tag="haochi"
  data-index="{{index}}"
 data-goods="{{item}}"
  desc="{{item.title}}"
  title="{{item.name}}"
  thumb="/assets/image/1.jpg"
  bindtap="showInfo" 
  lazy-load
>
</van-card>
</block>
